# Detailed module routing reference

This file is the detailed Route reference for [`AGENTS.md`](../../AGENTS.md). The root `AGENTS.md` remains the entry point for every agent run. Read this file when you need the complete module routing matrix, when you update routing, or when an active/historical plan mentions provider, tool, session, CLI, runtime, public-boundary, documentation, or pi-parity routing concepts. The historical section map below keeps older plan references to numbered `AGENTS.md` sections resolvable.

## How to use this reference

1. Start in [`AGENTS.md`](../../AGENTS.md): run the Start Gate checks, apply the Guardrails, and classify the task.
2. If the target seam is already obvious, go directly to the files listed in the matching row below.
3. If the target seam is unclear, or if you are editing route documentation, use this table as the source of truth for task family, preferred entry points, routing notes, and validation slice.
4. Keep edits minimal. Updating this reference must not change C++ behavior, build configuration, CLI behavior, provider behavior, tool behavior, runtime behavior, or session behavior.

## Route matrix

| Task family | Preferred entry points | Routing notes | Validation slice |
| --- | --- | --- | --- |
| Agent loop / turn flow / tool-call orchestration | `include/cch/agent/`, `src/agent/AgentLoop.cpp`, `src/agent/ToolCallExecutor.*`, `tests/agent/` | `AsyncAgentLoop` owns provider-neutral turn flow and rejects length-truncated tool intent before execution. The private `ToolCallExecutor` adapter owns accepted-call hooks, lifecycle events, sequential/bounded-parallel scheduling, error mapping, and source-ordered results. Tool adapters default to `Exclusive`; bounded parallelism requires explicit `ParallelSafe` and run-policy opt-ins. | `[agent][async]`, `[agent][tool-executor]`, and architecture tests when scheduling contracts change. |
| AI messages, content, tool schema, and usage contracts | `include/cch/ai/`, `tests/ai/` | Keep public message and content types as passive value contracts. Preserve aggregate-friendly domain structures. | `[ai]` tests; architecture tests if public types change. |
| OpenAI-compatible provider / SSE / HTTP transport | `include/cch/ai/providers/`, `src/ai/providers/`, `src/ai/glaze/`, `tests/ai/providers/` | Keep provider wire DTOs and Glaze mappings in implementation/serialization layers. Preserve the `StreamingChatClient` / `StreamTransport` seams. | `[ai][provider]` tests; do not run live provider validation by default. |
| Provider/model registry | `include/cch/ai/ProviderRegistry.hpp`, `src/ai/ProviderRegistry.cpp`, `src/ai/providers/FakeChatClient.*`, `include/cch/ai/providers/OpenAICompletionsCompat.hpp` | Register fake and OpenAI-compatible providers here. CLI/runtime resolves provider and model settings instead of hard-coding clients. | Provider registry tests; fake-provider smoke tests. |
| Agent config directory / user settings / model resolution | `include/cch/coding_agent/AgentConfigDir.hpp`, `include/cch/coding_agent/Settings.hpp`, `src/coding_agent/AgentConfigDir.cpp`, `src/coding_agent/SessionPathPolicy.*`, `src/coding_agent/SettingsLoader.cpp`, `src/coding_agent/ProviderConfigResolution.*`, `tests/coding_agent/AgentConfigDirTest.cpp`, `tests/coding_agent/SessionPathPolicyTest.cpp`, `tests/coding_agent/SettingsLoaderTest.cpp`, `tests/coding_agent/ProviderConfigResolutionTest.cpp` | `AgentConfigDir` is the single source of user-level roots, including the side-effect-free sessions root, mirroring pi's `~/.pi/agent/` (override: `CCH_CODING_AGENT_DIR`). Private `SessionPathPolicy` is the authoritative pure seam for pi-readable workspace keys, automatic UUID/timestamp identity, filenames, and target composition. `~/.cpp-harness/agent/settings.json` loads provider/model/base-url/api-key-env, `default_project_trust`, and project resource defaults. `resolve_provider_settings()` centralizes explicit/settings/stored/default precedence for both CLI and SDK paths; `ProviderRequest` is the internal generic explicit input. | Agent config dir, session path policy, settings loader, and provider resolution tests. |
| JSON serialization / deserialization | `src/ai/glaze/`, `src/util/Json.hpp`, `tests/ai/GlazeRoundTripTest.cpp` | Domain-facing APIs use `include/cch/util/JsonValue.hpp`. Keep Glaze DTOs and JSON adapters in `src/` implementation layers, converting only at physical boundaries. | `[ai]` round-trip tests; architecture tests if public types change. |
| Built-in tools: read/write/edit/bash | `include/cch/tools/ToolFactories.hpp`, `src/tools/AsyncToolFactories.cpp`, `tests/tools/` | Tools are exposed through `AsyncAgentTool`. File and process capabilities must go through the execution environment. When adding a tool, define the `ai::Tool` schema, implement `AsyncAgentTool`, register through `AsyncToolRegistry`, and add focused tests. | `[tools][async]` tests. |
| Slash commands / prompt processing | `src/coding_agent/runtime/AsyncCliRuntime.*` (CLI adapter built-in command dispatch), `src/coding_agent/prompt/PromptProcessor.*`, `src/coding_agent/prompt/PromptTemplateExpander.*`, `src/coding_agent/prompt/SlashCommandParser.*`, `tests/coding_agent/PromptProcessorTest.cpp`, `tests/coding_agent/PromptTemplateProcessingTest.cpp`, `tests/coding_agent/BuiltinCommandsTest.cpp`, `tests/cli/CliSmokeTest.cpp` | The CLI adapter resolves built-in slash commands before ordinary input reaches the session. `PromptProcessor` remains the sole interpretation interface for skill and prompt-template expansion: cached skill expansion flows into prompt-template expansion, and unmatched slash input passes through silently. SDK slash-command registration is not part of the `AgentSession` prompt path; `PromptTemplateExpander` and `SlashCommandParser` are private implementation modules; no prompt-processing free functions are published under `include/cch/`. | `[coding_agent][prompt]`, `[coding_agent][skill-integration]`, `[cli][commands]`, and adapter tests when prompt presentation changes. |
| Project trust / resource controls | `include/cch/coding_agent/ProjectTrust.hpp`, `include/cch/coding_agent/ProjectResources.hpp`, `src/coding_agent/ProjectTrust.cpp`, `src/coding_agent/ProjectResources.cpp`, `src/coding_agent/ProjectResourceLoader.*`, `tests/coding_agent/Project*Test.cpp`, `tests/cli/CliSmokeTest.cpp` | `ProjectResourceLoader` is the shared startup resource-loading seam. CLI, RPC, and SDK session creation consume loaded resources from this seam instead of scanning project directories at call sites. | Project trust/resource tests; CLI smoke test if CLI startup behavior changes. |
| Skills / resource loading | `include/cch/coding_agent/Skill.hpp`, `src/coding_agent/ProjectResourceLoader.*`, `src/coding_agent/SkillLoader.*`, `src/coding_agent/PromptTemplateLoader.*`, `src/coding_agent/SkillFormatting.*`, `src/coding_agent/runtime/AgentSessionRuntime.*`, `src/coding_agent/runtime/SessionFactory.*`, `tests/coding_agent/Skill*Test.cpp`, `tests/coding_agent/PromptTemplateLoaderTest.cpp`, `tests/coding_agent/ProjectResourceLoaderTest.cpp` | Project-local `.cpp-harness/skills/**/SKILL.md` parsing is delegated to `SkillLoader`; project-local `.cpp-harness/prompts/*.md` parsing is delegated to `PromptTemplateLoader`. Startup policy, trust gating, host/project duplicate precedence, and loader diagnostic normalization belong in `ProjectResourceLoader`. | Skill/template/loader tests. |
| Workspace, path guard, and shell execution | `include/cch/harness/ExecutionEnv.hpp`, `include/cch/harness/LocalExecutionEnv.hpp`, `src/harness/` (including private `SyncLocalExecutionEnv.*`), `src/tools/PathGuard.hpp`, `src/util/Process.*`, `tests/harness/` | This seam owns path containment and safety checks. Async shell I/O must go through `ProcessRunner`. Keep private execution-env implementation details out of public include surfaces. | `[harness]` tests; add tests for any new containment or redaction behavior. |
| Session Store capability / JSONL / resume / tree navigation | `include/cch/harness/session/SessionStore.hpp`, `include/cch/harness/session/JsonlSessionStore.hpp`, `src/harness/session/JsonlSessionStore.cpp`, `src/harness/session/SessionTree.cpp`, `src/harness/session/SessionResume.cpp`, `src/coding_agent/SessionPathPolicy.*`, `src/coding_agent/runtime/SessionLifecycle.*`, `src/coding_agent/runtime/AgentSessionRuntime.*`, `tests/harness/session/`, `tests/coding_agent/SessionPathPolicyTest.cpp`, `tests/coding_agent/runtime/SessionLifecycleTest.cpp`, `tests/coding_agent/SdkSessionTest.cpp` | `SessionStore` is Runtime's narrow incremental-append and path capability; do not widen it with metadata, serialization, journal, tree-navigation, or resume operations. `SessionPathPolicy` computes automatic persisted targets without filesystem effects; `SessionLifecycle` is the publication seam that privately creates/tightens default directories and constructs the JSONL store. Explicit custom publication does not change existing directory permissions. `AgentSessionRuntime` updates live history before subscribers and appends completed user, assistant, and tool-result messages after delivery; failures do not roll back live state or close the session. Resume remains concrete: `SessionResume` returns durable active-path history and `SessionTree` owns tree indexing, branch navigation, and compaction-aware context reconstruction. When adding session fields, preserve append-only JSONL and tolerate unknown entries. | `[coding_agent][session-path-policy]`, `[harness][session]`, `[sdk][live-state]`, `[sdk][incremental-persistence]`, and session lifecycle tests. |
| CLI / REPL / runtime wiring | `src/main.cpp`, `src/cli/` (`CliParse`, `CliPreflight`, `CliConfig`, `CliRuntimeConfig`), `src/coding_agent/runtime/AsyncCliRuntime.*` (frontend command dispatch and presentation), `src/coding_agent/runtime/SessionFactory.*`, `src/coding_agent/runtime/RuntimeServices.*`, `tests/cli/` | CLI11 argument parsing and preflight live in `src/cli/`. CLI session creation translates source-specific input into `SessionFactory`, which owns the single private normalization, validation, and assembly pipeline. `AsyncCliRuntime` resolves built-in slash commands in the CLI adapter before ordinary input reaches the session and owns command presentation/shutdown policy for text and JSON one-shot modes. `RuntimeServices` is a passive internal bundle assembled only by the factory. | `[cli]` tests; README build/run examples for manual smoke checks. |
| Embeddable SDK / host resources | `include/cch/coding_agent/Sdk.hpp`, `src/coding_agent/Sdk.cpp`, `src/coding_agent/runtime/AgentSessionRuntime.*`, `src/coding_agent/runtime/SessionFactory.*`, `src/coding_agent/ProjectResourceLoader.*`, `tests/coding_agent/SdkSessionTest.cpp` | Public SDK session creation translates source-specific options into `SessionFactory`, which owns shared provider, resume, trust, resource, tool, and runtime assembly. `AgentSession::prompt` returns only success or an explicit error; one persistent move-only subscription path carries progress, and accessors expose live state updated before subscriber delivery. Completed messages persist incrementally after delivery, and subscriber or persistence failures leave the session open with live history retained. Host-provided skills and prompt templates take precedence over project-discovered duplicates, resource diagnostics are returned as `CreateAgentSessionResult::diagnostics` values, and host-provided execution environments remain host-owned. | `[sdk]`, especially `[sdk][live-state]` and `[sdk][incremental-persistence]`; `[architecture]` when the public prompt or subscription contract changes. |
| JSON / RPC output modes | `src/coding_agent/runtime/JsonEventPrinter.*`, `src/coding_agent/runtime/RpcMode.*`, `src/coding_agent/runtime/RpcJsonl.*`, `tests/coding_agent/runtime/JsonEventPrinterTest.cpp`, `tests/coding_agent/runtime/RpcModeTest.cpp`, `tests/cli/CliSmokeTest.cpp` | `--mode json` writes the v3 session header followed by direct, redacted, bounded AgentSession events; it does not emit schema envelopes, sequence counters, content-status substitutions, or runtime terminal records. `RpcMode` owns RPC command acceptance, preflight response/event ordering, recoverable failures, and EOF/shutdown semantics. It emits prompt success after AgentSession preflight and before direct events, returns preflight rejection as an error response, and emits neither a startup header nor terminal records. Focused runtime tests protect those protocol rules; CLI smoke tests protect startup, stdin/stdout/stderr wiring, preflight and resume assembly, and startup-diagnostic separation. | `[coding-agent][runtime][rpc]`; `[coding-agent][json-events]` when event records change; `[cli][rpc]` when CLI startup or stream wiring changes. |
| Public boundary / architecture guards | `tests/architecture/`, `CMakeLists.txt`, `include/cch/` | `include/cch/` is reserved for domain contracts and abstract seams. Implementation details belong in `src/`, with CMake using `PRIVATE` includes. If you change public headers, dependencies, include surfaces, provider/tool/session contracts, or CMake public/private boundaries, run the architecture tests. | `./build/cpp_harness_tests "[architecture]"` |
| Documentation and tracker records | `README.md`, `CONTEXT.md`, `docs/agents/`, `docs/adr/`, referenced `.scratch/<effort>/spec.md` or ticket | Put current user behavior in README, project language in CONTEXT, agent routing here, hard-to-reverse decisions in ADRs, and work state in the local tracker. Do not create a separate implementation-plan layer. For docs-only maintenance, validate links, headings, tracker consistency, clear agent-facing English, and no-information-loss for migrated current facts. | Markdown link/heading and tracker-state checks; no C++ build unless code/build files changed. |

## Cross-reference anchors

- Provider, model, and OpenAI-compatible routing map to the provider rows above.
- Tool routing maps to the built-in tools row and, when shell/file capabilities are involved, the workspace/path/shell execution row.
- Session and resume routing map to the session JSONL / tree navigation row.
- CLI, REPL, JSON mode, RPC mode, and runtime routing map to the CLI/runtime and JSON/RPC rows.
- Public-boundary and architecture-sensitive routing map to the public boundary / architecture guards row.
- Documentation and pi-parity routing map to the documentation/tracker row plus the pi C++ parity guidance in [`AGENTS.md`](../../AGENTS.md) and the low-resolution map at [`.scratch/pi-cpp-parity/map.md`](../../.scratch/pi-cpp-parity/map.md).

## Historical AGENTS.md section map

Some active or historical plans refer to the pre-router numbered sections in `AGENTS.md`. Resolve those references to the current concepts instead of reintroducing section numbers:

| Historical reference | Current place |
| --- | --- |
| `AGENTS.md` §0 / `先读顺序` | `AGENTS.md` → Start Gate |
| `AGENTS.md` §1 / project positioning | `README.md` for behavior and safety context; `AGENTS.md` → Guardrails for always-on rules |
| `AGENTS.md` §2 or §2.3 / architecture rules | `AGENTS.md` → Guardrails, especially the move-only event-sink rule |
| `AGENTS.md` §2.5 / pi C++ parity route | `AGENTS.md` → pi C++ Parity Direction plus `.scratch/pi-cpp-parity/map.md`; deleted plans remain available only through Git history |
| `AGENTS.md` §3 / routing table or route row style | `AGENTS.md` → Route for compact dispatch; this file's Route matrix for detailed rows |
| `AGENTS.md` §4 / build and validation commands | `README.md` for build/test commands; `AGENTS.md` → Verify Slice for validation choice |
| `AGENTS.md` §5 / change rules | `AGENTS.md` → Guardrails and Verify Slice; this file's relevant route rows for tool, provider, session, workspace, and documentation-specific rules |
| `AGENTS.md` §5.5 / branch, merge, and PR conventions | `AGENTS.md` → Worktree Discipline plus [`docs/agents/worktree-discipline.md`](worktree-discipline.md) |
| `AGENTS.md` §6 / pre-handoff checklist | `AGENTS.md` → Verify Slice and Handoff |

## Maintenance rules

- Treat this file as the stable home for detailed module routing. Root `AGENTS.md` may summarize routes, but this file must preserve full task-family coverage.
- When adding, removing, or renaming a module seam, update the matching task family, preferred entry points, routing notes, and validation slice here first.
- Preserve the current architecture guardrails: public contracts stay passive, capabilities stay behind seams, lifecycle events remain weakly coupled, and generic/serialization machinery stays localized.
- Route-documentation changes are documentation-only unless the user explicitly asks for code changes.
- Do not route agents through a `docs/plans/` layer. Open decisions belong in a wayfinder map, approved work in tracker specs/tickets, durable decisions in ADRs, and current facts in README or this route reference.
