// Minimal composition entry point for the install-gate fixture: a real
// compiled+linked executable so the fixture exercises the same target kind as
// the production cpp_harness composition.
int main() {
    return 0;
}
