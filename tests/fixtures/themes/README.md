# Theme compatibility fixtures

`dark.json` and `light.json` are independent local compatibility fixtures copied from pi commit
`864b35c462f9623579b068e9cab848419f9e1d0f`. Tests load only these repository files and never
read pi configuration or source directories at runtime.
