#include "RuntimeServices.hpp"

namespace cch::coding_agent::runtime {

// RuntimeServices is a passive value bundle defined in the header. The
// SessionFactory is the only seam that assembles it.

} // namespace cch::coding_agent::runtime
