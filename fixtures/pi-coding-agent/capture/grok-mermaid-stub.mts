// The differential scenarios do not exercise Mermaid rendering. This stub is
// used only when a local frozen pi checkout omits the optional grok-mermaid
// package, so importing the Native TUI remains an offline evidence run.
export type Span = { cls: "none"; text: string };
export type MermaidArt = { plain: string[]; styled: Span[][]; width: number; warnings: string[] };
export function render(_source: string): null {
	return null;
}
